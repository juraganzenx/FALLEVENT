local Networking = require(game.ReplicatedStorage.SharedModules.Networking)

local BuyList = {
    -- Sprinkler
    "SSSSSyrup Sprinkler",

    -- Watering Can
    "SSSSSSyrup Watering Can",

    -- Magic Mail

    -- Other Gear
    "Harp",
}

while task.wait(2) do
    for i = 1, #BuyList do
        Networking.GearShop.PurchaseGear:Fire(BuyList[i])
    end
end
